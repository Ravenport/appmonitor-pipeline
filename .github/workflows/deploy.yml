name: CI Pipeline

on: 
  push:
    branches: 
      - main 

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: release
        run: echo "Deploying to $PROD_DOMAIN"